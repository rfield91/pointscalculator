# Requirements

- .net 6.0

# Generating Results

1. Copy the final results file into `/results`, making sure to use a consistent format across all files (e.g. `Event1.html`, `Event2.html`). 
2. Run the program using the `dotnet run` command.